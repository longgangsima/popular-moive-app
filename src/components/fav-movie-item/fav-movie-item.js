import React, {Component} from "react";
import {URL_IMAGE_PREFIX} from "../../constants";


export default class FavMovieItem extends Component {

    /*
    id: string
    poster_path: string or null
    overview: string
    release_data: string
    title: string
    isFavorite: boolean
    isBlock: boolean
    */
    render() {

        return (
            <div>

            </div>
        )
    }
}
